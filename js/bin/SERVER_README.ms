## Server Events

### Text Events

* `getfilelist` -- get a list of files
* `readfile` -- read a file and send it to the client
* `uploadfile` -- get a file and save it to filesystem
* `getservertmpdirectory` -- get a place to temporary write files
* `getserverbasedirectory` -- get the base directory of the server
*
### File From Client
