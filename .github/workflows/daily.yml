on: 
  schedule:
    - cron: '0 10 * * *'

jobs:
  test_job:
    runs-on: ubuntu-latest
    name: Regression Test
    steps:
    - name: Regression
      id: regression
      uses: bioimagesuiteweb/bisweb/actions/test@devel
